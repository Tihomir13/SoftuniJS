function NxNMatrix(n) {
    let str = ``;
    for (let i = 0; i < n; i++) {
        for (let p = 0; p < n; p++) {
            str += ` ` + n;
        }
        console.log(str);
        str = ``;
    }
}